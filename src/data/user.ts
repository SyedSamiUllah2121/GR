/**
 * The operator the branches belong to, shown at the foot of the rail and on
 * the sign-in screen.
 *
 * The signed-in user used to live here too, as a single hardcoded profile.
 * Accounts are real now — read them with `useCurrentUser`, backed by
 * services/session.ts and services/userStore.ts.
 */
export const ORGANISATION = {
  name: 'Royal Gujrat',
  tagline: 'Restaurant & Sweets',
};
