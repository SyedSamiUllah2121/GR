import type {NextConfig} from 'next';

const nextConfig: NextConfig = {
  // Inspection records are stored in the browser (localStorage), so there is no
  // server-side data layer to configure here.
};

export default nextConfig;
